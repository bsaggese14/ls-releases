This repo contains releases for LaborSight. The repo is public because that is a requirement for the electron auto-updater to find the releases and then auto-download them for the user.

Make sure the exe file for windows uses dashes instead of periods. It should be LaborSight-Setup-0.0.4.
